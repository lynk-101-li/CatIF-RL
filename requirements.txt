# Pip-only install path (CUDA 11.7 wheels for PyTorch).
# For a self-contained conda environment instead, use environment.yml.
#
# CUDA toolkit / OpenMM / DSSP / pdbfixer are not pip-installable;
# install them via conda or your system package manager before `pip install -r`.

torch==2.0.0
torchvision==0.15.1
torchaudio==2.0.1
torch-geometric==2.4.0
einops==0.8.1
ema-pytorch==0.7.7
biopython==1.83
mdanalysis==2.9.0
mrcfile==1.5.4
rdkit==2024.9.6
scikit-learn==1.6.1
scipy==1.15.2
numpy==1.24.3
pandas==2.2.3
matplotlib==3.10.1
seaborn==0.13.2
pillow==11.2.1
tqdm==4.65.0
pyyaml==6.0
networkx==3.4.2
